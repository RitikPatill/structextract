# LLM clients
anthropic>=0.28,<0.29
openai>=1.30,<1.31

# Document parsing (used in M3 — listed here so the dep set is declared once)
pdfplumber>=0.11,<0.12
beautifulsoup4>=4.12,<4.13
markdown-it-py>=3.0,<3.1

# Web API (M4)
fastapi>=0.111,<0.112
uvicorn[standard]>=0.30,<0.31

# Data validation
pydantic>=2.7,<2.8

# Dev / testing
pytest>=8.2,<8.3
pytest-asyncio>=0.23,<0.24
httpx>=0.27,<0.28          # for FastAPI test client
